/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['nextui.org', 'firebasestorage.googleapis.com', 'avatars.githubusercontent.com']
    }
}

module.exports = nextConfig
